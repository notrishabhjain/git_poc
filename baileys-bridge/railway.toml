[build]
builder = "DOCKERFILE"
dockerfilePath = "baileys-bridge/Dockerfile"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "baileys-bridge"
internalPort = 3001
